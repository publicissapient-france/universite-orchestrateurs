version: "3"

services:
  web:
    image: xebiafrance/uo-click-count
    depends_on:
      - redis
    ports:
      - "8080:8080"
  redis:
    image: redis
    command: redis-server --appendonly yes
    volumes:
      - data-volume:/data

volumes:
  data-volume: