---
- name: Common bootstrap tasks
  hosts: all
  become: true
  tasks:
    - name: Ensure hostname set
      hostname: name={{ inventory_hostname | replace("_", "-")}}

- include: 20_swam.yml

- include: 40_monitoring.yml